# PR: S-05 /start — 동의·문진 + 링크 발급 (시안 2c + 1a)

브랜치 제안: `feat/start-consent-issue`
커밋 메시지 제안: `feat(start): S-05 동의 모달 + Q1 문진 + 링크 발급 (시안 2c/1a)`

## 적용 방법
`handoff/src/app/start/page.tsx` → `src/app/start/page.tsx` (새 파일 하나). keyframes·토큰은 main에 이미 있습니다.

## 무엇이 들어가나 — 두 국면이 한 파일
**1/2 intake**
- 왼쪽 단계 레일(동의 → 무슨 일 → 링크 발급), 상태 따라 앰버/파랑 전환
- 동의 전문 모달: 요약 4카드 + 조항 5개 「확인했습니다」 체크, 5/5에서 동의 활성
- Q1 문진 3택 라디오(동작함, 모름 상시) · 종류별 업로드 슬롯 4개
- [다음]/[건너뛰고 바로 시작] 같은 크기

**2/2 issued (발급 1a)**
- [다음] → 발급 화면 전환. 오렌지 글로우는 이 순간에만
- URL 카드(break-all·글로우) + [주소 복사] — clipboard 동작, "복사됨 ✓" 피드백
- 이메일 입력(검증 없음) + 앰버 경고 같은 크기 나란히 (ADR-021)
- [저장하고 시작하기]/[이메일 없이 시작하기]

## TODO (코드 내 주석 표시)
- Q1 선택 → POST /api/cases §3.1 연결 (지금은 UI 상태만)
- CASE_URL 상수 → 발급 응답 토큰으로 교체
- 저장/시작 버튼 → /c/{token} 라우팅
- 업로드 슬롯 채택 시 POST …/evidence 에 kind 필드 협의

## 팀 확인 필요
- `REQUIRE_ALL_CHECKS`: 5조항 체크 관문 — 「관문은 동의 하나뿐」 원칙과 긴장. false로 끄면 체크 없이 동의 가능
- 약관 문구는 법무 검토 전 초안 (ADR-016·026 기준)
- 발급 시안 1b의 QR(폰으로 가져가기)은 미채택 — 추후 원하면 qrcode 라이브러리 필요
