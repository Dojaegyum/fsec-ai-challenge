/**
 * 시험에서 `server-only` · `client-only` 를 대신하는 빈 모듈.
 *
 * 두 패키지는 잘못된 런타임에서 import 되면 오류를 던지도록 만들어져 있습니다.
 * 그것이 목적이고(ADR-021 다섯), 실제 빌드에서는 그대로 작동해야 합니다.
 *
 * 다만 시험은 Node 에서 도는데 브라우저 모듈(pii-restorer 등)도 시험해야 하므로,
 * vitest.config.mts 가 이 파일로 바꿔치기합니다. **경계 검사를 끄는 것이 아니라
 * 시험 실행기에서만 비활성화하는 것입니다** — `next build` 는 여전히 막습니다.
 */

export {}
