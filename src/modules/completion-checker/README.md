# completion-checker

**완료를 사용자의 체크가 아니라 부산물로 판정합니다.**

| | |
| --- | --- |
| 계약의 정본 | [05-completion-hook.md](../../../spec/backend/08-14-completion-hook.md) |
| 저장 구조 | [09-data-model.md](../../../spec/backend/08-16-data-model.md) §7 |
| 응답 형태 | [08-api.md](../../../spec/common/08-14-api.md) §3.8 |
| 근거 | [ADR-028](../../../decisions/028-runtime-and-module-shape.md) |

## 왜 이 모듈이 있나

**체크리스트는 "체크는 됐는데 행위는 안 된 상태"를 막지 못합니다.** 그리고 은행·경찰
시스템에 조회 API가 없어 완수를 직접 확인할 수도 없습니다.

그런데 모든 공식 절차는 마치면 **부산물**을 남깁니다 — 112 신고는 사건접수번호를,
지급정지는 은행 접수 문자를, 피해구제 신청은 접수증을. 그 부산물을 완료 조건이자
**다음 단계의 필수 입력**으로 삼으면, 인위적 잠금이 아니라 **절차 자체의 의존관계**로
완수가 강제됩니다.

`CLAUDE.md` 불변 규칙 6이 이것입니다.

## 낸 형태가 곧 검증 레벨입니다

| 사용자가 낸 것 | 레벨 | 결과 | 단계 상태 |
| --- | --- | --- | --- |
| 접수번호를 직접 입력 | **L1** | 형식이 맞으면 `passed` | `done_verified` |
| | | 틀리거나 형식을 모르면 `failed` | `in_progress` |
| 캡처·서류를 올림 | **L2** | 판독 글에 **「접수번호」 자리**나 **공공기관 이름**이 있으면 `passed` | `done_verified` |
| | | 둘 다 없음 · 읽기 실패 · 통화 녹음이면 `failed` — **올렸지만 확인 못 함** | **`unconfirmed`** |
| | | 아직 읽는 중이면 `not_applicable` · `reading_pending` — 읽기가 끝나면 다시 판정 | **`unconfirmed`** |
| 했다고만 말함 | **L3** | `not_applicable` | **`unconfirmed`** |

## L2 — 올린 것 자체는 증빙이 아닙니다

→ [ADR-077](../../../decisions/077-upload-is-not-proof.md)

**2026-09-06 까지는 파일이 올라왔다는 사실만으로 통과였습니다.** 사기범과의 통화 녹음을 올려도
「신청서류 제출」이 끝났고, 이체 캡처가 112 접수증으로 인정됐습니다. 불변 규칙 6 이 말하는 근거는
「절차가 남긴 것이 **존재한다**」이지 「무엇이든 올렸다」가 아닙니다.

이제 `verify()` 는 `evidence`(종류·읽기 상태·**토큰화된 판독 글**)와 `orgNames`(공공기관 이름·별칭)를
받아 `findReceiptMarks()` 로 봅니다.

| 무엇 | 어떻게 |
| --- | --- |
| **접수번호 자리** | 접수번호·사건번호·신고번호·민원번호·관리번호·처리번호·공고번호·접수 No. 뒤에 `looksLikeReceiptNumber` 를 통과하는 번호 — 또는 가린 이름표(`[계좌-1]`). 자리 없이 숫자만 있는 것(날짜·금액)은 아닙니다 |
| **기관명** | `orgNames` 를 글자 그대로 찾습니다. 숫자뿐인 별칭(`112`·`1332`)과 한 글자는 안 봅니다 — 금액·시각에 섞입니다 |
| **은행 이름** | **근거가 아닙니다.** 이체 캡처에도 있습니다. 부르는 쪽이 `orgNames` 에 공공기관만 넣습니다 |
| 통화 녹음 | 서류가 아닙니다 — 안에 「접수번호」가 들리더라도 `not_a_document` |

판독은 올린 직후에 안 끝나 있는 것이 보통이라, 라우트는 `reading_pending` 으로 적어 두고
읽기가 끝난 자리(`flows/settle-artifacts.ts`)가 **같은 `verify()`** 로 판정을 마칩니다.

## L3만으로 완료가 되지 않습니다

**이 규칙이 무너지면 이 모듈의 존재 이유가 사라집니다.** 그래서 시험 하나를 따로 두어,
어떤 설정으로 만들어도 자기 신고가 `done_verified`가 되지 않는지 확인합니다.

`unconfirmed`는 **종결 상태가 아닙니다.** 리마인더 추적 대상으로 남습니다.

`verifyResult`가 `not_applicable`인 것은 실패가 아니라 **검증할 것이 없었다**는 뜻입니다.
사용자는 할 수 있는 것을 다 했으므로 다음 선택지를 내밀지 않습니다.

## L1이 실패해도 길을 막지 않습니다

실패하면 단계가 `in_progress`로 남고(뒤로 가지 않습니다), **다음 길을 함께 냅니다.**

```
{ level: 'L2', label: '접수 문자 캡처를 올려주세요' }
{ level: 'L3', label: '번호 없이 접수했다고 표시' }
```

**막다른 길을 만들지 않는 것**이 규칙입니다. 패닉 상태의 사용자를 막아 세우면 이탈합니다.

## L1 은 「형식이 맞나」가 아니라 「받아 적었나」를 묻습니다

→ [ADR-057](../../../decisions/057-receipt-number-l1.md)

**형식의 정본이 없습니다.** 112·은행·금감원 어디에도 공개된 규격이 없고, 흔히 보이는
`2025-000000` 꼴은 사설 안내에만 나옵니다 ([U-18](../../../docs/research/05-미확인-목록.md)).
**그리고 기다리지 않기로 했습니다** — 없는 것을 기다리는 동안 L1 이 죽어 있었습니다.

> 뜻을 바꾼 것이지 기준을 낮춘 것이 아닙니다. **불변 규칙 6이 말하는 근거는
> 「형식이 맞다」가 아니라 「그것이 존재한다」**이고, 접수번호는 절차를 밟지 않으면
> 생기지 않습니다. 형식 대조는 **할 수 있을 때만 얹는 덤**입니다.

### 두 겹으로 봅니다

**① 모양** — `looksLikeReceiptNumber()`. 구분자(`- – — . / _ ( )` · 공백)를 떼고 남은 것이
**영숫자 6자 이상**이고 **숫자가 하나라도** 있어야 합니다.

```
2026-004821        -> 2026004821       통과
KB-20260826-0001   -> KB202608260001   통과
9                  -> 9                거름 (짧다)
ㅇㅇ                -> ㅇㅇ              거름 (영숫자가 아니다)
```

⚠️ **이건 형식 규격이 아니라 오타·빈칸 거르개입니다.** 어느 기관의 형식도 주장하지
않습니다 — 주장하면 `CLAUDE.md` 의 「모르는 수치를 지어내지 마세요」를 어깁니다.

**② 형식** — `ReceiptNumberFormat`. 아는 기관만 대조합니다.

| 응답 | 뜻 | 결과 |
| --- | --- | --- |
| `true` | 형식에 맞다 | `passed` |
| `false` | 형식과 다르다 | `failed` · `format_mismatch` |
| `undefined` | **그 기관의 형식을 아직 모른다** | **`passed`** · `format_unchecked` |

**`undefined` 가 이제 통과입니다.** 모른다고 막아 세우면 제대로 받아 적은 피해자가
실패 화면을 봅니다 — 그 사람은 할 일을 다 한 사람입니다.

**통과에도 이유를 남깁니다.** 형식을 대조한 통과와 모양만 본 통과는 다른 일이고,
형식 정본이 생기는 날 **다시 볼 것을 `format_unchecked` 로 셉니다.**

## 검증 상세에 개인정보를 담지 않습니다

`verifyDetail`에는 `reason` 하나만 들어갑니다. 사용자가 입력한 값은 개인정보일 수 있고,
업로드 식별자도 담지 않습니다 → [09-data-model.md](../../../spec/backend/08-16-data-model.md) §7.

## 이 모듈이 하지 않는 것

- **다음 단계가 열렸는지 계산하지 않습니다.** 응답의 `unlocked_steps`는 플랜 구조를
  알아야 해서 이 모듈 밖입니다.
- **OCR 을 부르지 않습니다.** 판독은 전사 경로(`flows/read-evidence.ts`)가 돌리고, 이 모듈은
  그 결과 글을 받아 볼 뿐입니다. 이름·날짜까지 대조하는 것(정본 L1 의 원래 뜻)은 아직 없습니다 —
  **지금은 접수번호 자리와 기관명 유무까지입니다.**
- **저장하지 않습니다.** 판정만 돌려줍니다. 미뤄 둔 판정을 찾는 열쇠(`evidence_id`)를 표에 적는 것도 라우트의 몫입니다.

## 쓰는 법

```ts
import { createCompletionChecker } from '@/modules/completion-checker'

const checker = createCompletionChecker({ receiptFormat })

const verdict = checker.verify({
  submission: { kind: 'receipt_no', value: '2026-1234567' },
})
// { verifyLevel: 'L1', verifyResult: 'passed', stepState: 'done_verified' }
```

## 판단이 필요했던 자리

| 무엇 | 어떻게 | 왜 |
| --- | --- | --- |
| **형식을 모를 때** | **모양만 보고 통과시킨다** ([ADR-057](../../../decisions/057-receipt-number-l1.md)) | 정본이 없는데 막아 세우면 제대로 받아 적은 피해자가 실패 화면을 봅니다. 대신 `format_unchecked` 를 남겨 나중에 재검증 대상을 셉니다 |
| **L1 실패** | 단계는 `in_progress`, 다음 길을 함께 | 막다른 길을 만들면 패닉 상태의 사용자가 이탈합니다 |
| **L3 결과값** | `not_applicable` (실패 아님) | 검증할 것이 없었다는 뜻입니다. 사용자는 할 수 있는 것을 다 했습니다 |
