/**
 * L2 가 완료로 기록하지 못했을 때의 말 — **서버 판정과 화면이 같은 표를 씁니다.**
 *
 * 정본: spec/common/08-14-api.md §3.8 「응답 200 — L2 확인 못 함」
 * 근거: ADR-077(L2 는 판독 결과로 판정한다)
 *
 * ## 왜 표를 따로 뽑았나
 *
 * 서버는 부산물을 낸 **그 순간**의 판정과 함께 `note` 를 냅니다. 그런데 파일은
 * 그때 아직 읽는 중이라(`reading_pending`) 판독이 끝난 뒤 서버가 **다시**
 * 판정하고, 그 결과는 `verify_reason` 으로만 옵니다 — 화면이 그 값으로 안내를
 * 갈아끼워야 합니다(§3.6 `artifacts[].verify_reason`).
 *
 * 그러려면 같은 글자가 두 곳에 필요합니다. 양쪽에 적어 두면 **한쪽만 고쳐도
 * 아무 시험이 안 깨진 채 두 화면이 다른 말을 합니다.** 그래서 표는 여기 한
 * 벌이고, 서버(`verify.ts`)와 화면(`app/c/[token]/artifact.ts`)이 함께 읽습니다.
 *
 * ⚠️ **이 파일은 `index.ts` 를 지나지 않고 직접 import 됩니다** — 모듈의 공개
 * API 는 `server-only` 라 브라우저가 못 읽기 때문입니다(RFC-001 「모듈 하나의
 * 파일 골격」의 예외). 그래서 여기에는 **순수 상수만** 둡니다: 판정 로직도,
 * 서버에서만 되는 import 도 넣지 마세요.
 */

export const L2_NOTES = {
  reading_pending: '올린 자료를 읽는 중입니다. 접수번호가 보이면 완료로 기록합니다',
  unreadable: '올렸지만 읽지 못했습니다. 접수번호를 적어 주시면 확인합니다',
  not_a_document:
    '통화 녹음은 접수증이 아닙니다. 접수증이나 접수 문자 캡처를 올리거나 접수번호를 적어 주세요',
  no_receipt_marks: '올렸지만 접수번호를 찾지 못했습니다. 접수번호를 적어 주시면 확인합니다',
  /** 기관 이름만 보이는 경우 — 접수번호가 없으면 완료로 기록하지 않습니다 (ADR-083) */
  org_only: '기관 이름은 보이지만 접수번호가 없습니다. 접수번호를 적어 주시면 완료로 기록합니다',
} as const

/** 안내가 있는 이유들 — 화면이 받은 `verify_reason` 이 이 표에 있는지 봅니다 */
export type L2Reason = keyof typeof L2_NOTES
