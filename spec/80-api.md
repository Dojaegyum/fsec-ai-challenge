# 80 · API 계약

**미작성.** 기획서 v1.2에 엔드포인트 수준의 계약이 없어 추출할 것이 없습니다.
구현 착수 시 여기에 정의하고, 방식이 갈리면 먼저 [rfc/](../rfc/)에 제안을 씁니다.

## 기획서에서 이미 정해진 제약

API를 설계할 때 지켜야 하는 것들입니다. 이건 정해져 있습니다.

- **PII 경계** — 외부 LLM으로 나가는 페이로드는 토큰화된 텍스트만. 스크러버를 우회하는 경로를 만들 수 없습니다 → [40](40-pii-boundary.md)
- **복원 매핑은 서버에 저장하지 않음** — 서버 API는 토큰만 다룹니다
- **세션 격리 · TTL 24h** — 사건 스토어의 수명
- **감사 로그** — 모든 LLM 호출 기록 (토큰화 텍스트 기준)
- **KB 인용 강제** — 플랜 생성 응답은 근거·시행일을 함께 반환해야 합니다 → [70](70-kb-operations.md)

## 기획서에 등장하는 서버 구성 요소

이름만 나와 있고 계약은 없습니다.

| 구성 요소 | 역할 |
| --- | --- |
| API Gateway | 진입 |
| Ingest 서비스 | STT(화자분리)·OCR |
| 2차 PII 스크러버 | NER → 토큰화. **격리 경계** |
| 분석 오케스트레이터 | 파이프라인 조율 |
| 슬롯 체커 | 필수 정보 검사 → [20](20-slot-tiering.md) |
| Case Store | 세션 격리 · TTL 24h |
| 감사 로그 | 토큰화 텍스트 기준 |

## TODO

- TODO(미정): 엔드포인트 목록과 요청/응답 스키마
- TODO(미정): 업로드 방식 (직접 업로드 vs presigned)
- TODO(미정): 전사·분석의 진행 상태 전달 방식 (폴링 vs 스트리밍) — S-01의 "실시간으로 보이는" 요구와 직결
- TODO(미정): 에러 계약
