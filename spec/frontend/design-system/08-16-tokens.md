# 디자인 토큰 — 의미론

> **뼈대만 작성됨.** 아래 표의 값은 아직 정해지지 않았습니다.
> 값의 정본은 `src/app/globals.css`이고, 여기에는 **토큰 이름과 뜻**만 적습니다.

## 이미 계약으로 박힌 것

spec에서 이미 요구하고 있어 **협상 대상이 아닌** 것들입니다.

| 무엇 | 요구 | 출처 |
| --- | --- | --- |
| PII 토큰 | 전사 화면에서 **파란 토큰**으로 치환되는 것이 보여야 함 | [screens S-01](../08-14-screens.md) · [pii-boundary](../../common/08-14-pii-boundary.md) |
| 미확정 정보 | 숨기지 않고 **'미확인' 배지**로 드러냄 | [screens](../08-14-screens.md) |
| 경유 서비스 | 감지 결과를 **배지**로 표시 (CH-* 8유형) | [channel-matrix](../../backend/08-14-channel-matrix.md) |

## 정해야 할 것

### 상태 색 — 무엇을 뜻하는가

| 토큰(가칭) | 뜻 | 쓰이는 곳 | 값 |
| --- | --- | --- | --- |
| `--color-pii` | 가려진 개인정보 | 전사 뷰 토큰 | TODO |
| `--color-risk-high/mid/low` | 수법 위험도 (F-04) | 분석 결과 | TODO |
| `--color-deadline-urgent` | 법정 기한 임박 (3영업일) | D-day 트래커 | TODO |
| `--color-verified` | 부산물로 완수 확인됨 (F-06b) | 실행 보드 | TODO |
| `--color-unknown` | 미확인 슬롯 (T1/T2) | 배지 | TODO |

- TODO(정해야 함): 위험도를 **색만으로 구분하지 않는 방법**. 색맹 사용자에게 빨강/노랑 대비는
  작동하지 않습니다 — 아이콘·텍스트를 함께 씁니다.
- TODO(정해야 함): 다크 모드를 지원할지. shadcn은 `.dark` 변형을 이미 갖고 있습니다.

### 타이포

- TODO(정해야 함): 본문 최소 크기. **고령 사용자가 주 대상**이라 기본값보다 커야 합니다
  ([screens](../08-14-screens.md)의 미해결 TODO).
- TODO(정해야 함): 한국어 폰트. 현재 `--font-sans`는 스캐폴딩 기본값입니다.
- TODO(정해야 함): 숫자·기한 표시용 서체(기한 오독은 곧 피해입니다).

### 간격·모서리

- shadcn의 `--radius` 스케일을 그대로 씁니다. 바꿔야 할 이유가 생기면 여기에 적습니다.
- TODO(정해야 함): 3-패널 레이아웃의 패널 간격·최소 폭.
