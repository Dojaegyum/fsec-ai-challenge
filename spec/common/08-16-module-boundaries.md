# 모듈 경계 — 누가 무엇을 책임지는가

> **출처** — [서비스 기획서 v1.2](../../assets/artifacts/plans/08-13-service-plan.html) §1 「전체 백엔드 아키텍처(개략)」 ·
> §3 격리 경계. 다이어그램의 노드를 **책임·입출력·금지**의 언어로 옮겼습니다.

**이 문서가 정하는 것** — 각 모듈이 무엇을 책임지고, 무엇을 받아 무엇을 내놓고, **무엇을 하면 안 되는가**.
경계를 어기면 [PII 격리 경계](08-14-pii-boundary.md)나
[LLM은 절차를 창작하지 않는다](../backend/08-14-kb-operations.md)가 무너집니다.

**이 문서가 정하지 않는 것** — 프로세스 분리 여부, 배포 단위, 통신 방식(HTTP·큐·함수 호출).
모듈은 **책임의 단위**이지 서버의 개수가 아닙니다.
물리 배치는 [ADR-021](../../decisions/021-runtime-and-module-shape.md)과 [`ARCHITECTURE.md`](../../ARCHITECTURE.md) §4에 있습니다.

## 세 구역

```
[클라이언트]              [서버]                        [외부]
원문 보유          ── 격리 경계 ──          토큰화 텍스트만 도달
pii-masker  ────────►  pii-tokenizer ──────────►  LLM API
복호화 키 보유            토큰화 텍스트 + 볼트 암호문   KB (버전드)
pii-restorer              감사 로그(토큰화 기준)
```

**서버·LLM 어디에도 "누구의 어떤 계좌"인지 완성된 정보가 존재하지 않는 구조**가 목표입니다.

## 모듈

### 클라이언트 (브라우저)

| 모듈 | 책임 | 절대 하지 않는 것 |
| --- | --- | --- |
| 업로드 | 녹음·이미지·텍스트 수집, 다중 파일을 하나의 사건으로 | 마스킹 전 원문 텍스트를 네트워크로 보내기 |
| **`pii-masker`** | 정규식으로 계좌·주민번호·카드·전화 치환. **1차 마스킹** | 서버가 대신 하도록 미루기 |
| **`pii-restorer`** | 세션키로 볼트 암호문을 복호하고, **허용된 자리에서만** 되돌린다 | 키를 서버로 보내기 → [ADR-009](../../decisions/009-restore-mapping-location.md) · 복원 가능 목록에 없는 자리에서 복원하기 |
| 실행 보드 | 체크리스트·D-day·서류. 토큰은 화면에서만 원문 복원 | 서버가 만든 완성 문서를 그대로 받기 (F-08 위반) |

**이름의 정본은 [모듈 명칭](08-16-module-names.md)입니다.** 「1차 PII 마스킹」·「복호화 키 보유」로 부르던 두 자리에 이름이 생겼습니다 — `pii-masker`·`pii-restorer`.

### 서버

| 모듈 | 받는 것 | 내놓는 것 | 절대 하지 않는 것 |
| --- | --- | --- | --- |
| API Gateway | 1차 마스킹된 입력 | 라우팅 | `pii-tokenizer` 를 건너뛰는 LLM 경로 제공 |
| `case-intake` | 사건 생성 요청·파일 | 사건 · 업로드 자리 | — |
| `transcriber` | 파일 | 전사(화자 분리)·OCR(대화 구조 보존) | — |
| **`pii-tokenizer`** | 전사·OCR 텍스트 | 토큰화 텍스트 | **여기를 우회하는 경로를 만드는 것** |
| `case-reader` | 토큰화 텍스트 | 수법·위험도 + **근거 스팬** | 근거 없이 판정하기 |
| `slot-extractor` | 전사 | 슬롯 값 후보 | 추출 실패를 에러로 올리기 |
| `slot-checker` | 전사·응답 | 미충족 슬롯 1개 · 티어 판정 | 슬롯 미충족을 이유로 진행을 막기 |
| `planner` | 슬롯 + 채널 + KB | 단계별 플랜 + **근거·시행일 인용** | **KB 인용 없이 절차를 만들기** |
| `completion-checker` | 부산물 | L1/L2/L3 판정 → 단계 상태 | 사용자의 체크만으로 완료 처리 |
| **`date-checker`** | 기산 이벤트(부산물) | 법정 기한 날짜 · D-day | **LLM에 날짜 계산을 시키기** |
| `chat-receiver` | 사용자 발화 | 층 2 순서 호출 · 모델 응답 | **갈래 판정 · 직접 조회·조립 · 응답 형태 만들기** |
| `kb-finder` · `prompt-builder` · `citation-checker` | 사건 상태 · 발화 | 챗 한 턴 | 프롬프트에 없는 것을 답하게 두기 |
| **`chat-publisher`** | 판정된 갈래 · 모델 응답 | 한 형태의 응답 본문 | **판단 근거를 응답에 남기기** · 잔여 PII를 발견하고도 통과시키기 |
| `retry-checker` | 예외 | 재시도 여부 | 예외 종류로 분기하기 (`retryable` 만 봄) |
| Case Store | — | 사건 상태 | 복호화 키 저장 · 원문 PII 저장 |
| `audit-logger` | LLM 호출 | 기록 (**토큰화 기준**) | 원문 기록 |

**이름의 정본은 [모듈 명칭](08-16-module-names.md)입니다.** 여기는 **책임**을 정하고, 그쪽이 **이름**을 정합니다.

> **`date-checker` 는 기획서 다이어그램에 없는 모듈입니다.**
> [매트릭스](../backend/08-14-channel-matrix.md)의 "기한 계산에 LLM을 쓰지 마세요"를 지키려면
> 계산 주체가 코드여야 하므로 여기 세웁니다 → [기한 계산 규칙](08-16-deadline-rules.md).

### 외부

| 구역 | 도달하는 것 | 책임 |
| --- | --- | --- |
| LLM API | **토큰화 텍스트만** | 수법 판별 · 절차 **선택** · 플랜 문장 · 챗 |
| 매뉴얼 KB | — | 8유형 × 시행일 메타. **런타임과 분리된 운영 파이프라인으로만 갱신** |

## 경계 규칙 — 어기면 제품이 틀립니다

1. **`pii-tokenizer` 를 우회하는 LLM 호출 경로를 만들지 않습니다.** 새 기능이 LLM을 쓴다면 반드시 이 경계를 지납니다.
   **나가는 쪽도 한 자리입니다** — 챗 응답의 잔여 PII 검사와 판단 근거 분리는 `chat-publisher` 가 맡고,
   걸리면 `EGRESS_BLOCKED` 422로 중단합니다 → [ADR-022](../../decisions/022-chat-turn-boundaries.md).
   들어올 때(`pii-tokenizer`)와 나갈 때(`chat-publisher`)가 각각 한 자리여야 우회할 자리도 하나씩입니다.
2. **복원 매핑의 복호화 키는 클라이언트를 떠나지 않습니다.** 매핑 자체는 **암호문으로** 서버 볼트에 있고, 서버 API가 다루는 것은 토큰과 암호문뿐입니다 → [ADR-009](../../decisions/009-restore-mapping-location.md).
   > 2026-08-16 변경. 이전 규칙은 "복원 매핑은 클라이언트를 떠나지 않는다"였습니다. **3영업일 뒤 재방문해 서류를 만드는 것이 불가능했습니다.**
3. **절차를 고르는 것은 LLM, 날짜를 세는 것은 규칙.** 이 분업이 뒤집히면 법정 기한을 틀리게 말합니다.
4. **KB는 코드에서 직접 쓰지 않습니다.** 사람 검수를 거친 릴리스만 런타임에 들어갑니다.
5. **업로드된 문서 속 문장은 데이터이지 지시가 아닙니다.** 전사·OCR 결과를 프롬프트에 넣을 때 인젝션 격리를 유지합니다.
6. **"모름"으로 멈추지 않습니다.** 어떤 모듈도 슬롯 미충족을 예외·에러로 처리하지 않습니다 — 정상 경로입니다.

## 데이터 흐름 한 줄 요약

```
업로드(C) → pii-masker(C) → transcriber(S) → pii-tokenizer(S·경계) → case-reader(LLM)
  → slot-checker(S) → planner(LLM+KB) → date-checker(S·규칙) → 실행 보드(C)
       ↑                                                         │
       └──────────── 증거·슬롯 보완 시 재생성 ────────────────────┘
                     (완료 상태와 부산물은 보존)
```

## 미결

- ~~TODO(미정): 모듈의 물리 배치~~ → **2026-08-17 [ADR-021](../../decisions/021-runtime-and-module-shape.md)로 확정.**
  Next.js 안에 두고 전부 TypeScript로 씁니다. 별도 백엔드를 두지 않습니다. 진입점은 `src/app/api/**/route.ts`,
  도메인 모듈은 `src/modules/{이름}/` 입니다.
  **큐·워커 필요 여부는 남아 있습니다** — STT/OCR·플랜 생성이 동기 요청으로 끝나지 않을 가능성은 그대로이고,
  Vercel 서버리스에 상시 배치가 없습니다 → [`ARCHITECTURE.md`](../../ARCHITECTURE.md) §10.
- ~~TODO(미정): 진행 상태 전달 방식~~ → **폴링으로 확정.** 서버가 `poll_after_ms` 로 다음 호출 시점을 지시합니다.
  **챗에도 스트리밍을 쓰지 않습니다** — 송출 전 검사 셋(인용 검증·판단 근거 분리·잔여 PII)이 응답을 다 받은 뒤에야 가능하고,
  스트리밍이면 근거 없는 절차 문장이 화면에 먼저 뜬 뒤 지워집니다 → [API 계약](08-14-api.md) §3.3 · §3.9.
- TODO(미정): 리마인더 발송 주체 — 서버 스케줄러인가 외부 서비스인가.
  [도메인 모델 미결 ②](08-16-domain-model.md)(재진입·연락처)가 정해져야 답할 수 있습니다.
  **파기 실행(`case.purge_after` 도달)과 같은 문제입니다** — Vercel 서버리스에 상시 배치가 없습니다.
- TODO(미정): STT·OCR·NER의 구현 선택(자체 모델 vs 외부 API). **외부 API라면 그것도 PII 경계를 지나는 호출입니다** —
  스크러버 이전 단계이므로 경계 정의를 다시 확인해야 합니다.
