# 의사결정 기록 (ADR)

되돌리기 어렵거나 나중에 "왜 이렇게 했더라"가 나올 결정을 한 건에 한 파일로 남깁니다.
주제 확정, 기술 스택, MVP 범위 컷 같은 것들이 대상입니다.

여기는 **왜 그렇게 정했나**를 남기는 곳입니다(과거형·이력). 그래서 **지금 지켜야 할 규칙**은
[rfc/](../rfc/)에 따로 있습니다(현재형·현행).

**한 결정이 둘 다 낳는 것이 정상입니다** — 판단은 여기, 그 결과 지킬 규칙은 `rfc/`,
제품이 만족해야 할 계약은 [spec/](../spec/). 셋을 같이 갱신하는 것까지가 한 작업입니다.

**ADR은 지우지도 고치지도 않습니다** — 뒤집을 때는 새 번호를 쓰고 기존 것을 `대체됨`으로 표시합니다.

## 파일명과 번호

**파일명은 `NNN-{slug}.md`** — 번호가 곧 ID입니다 (→ [RFC-001](../rfc/001-repo-structure.md)).

```
파일   decisions/001-topic-selection.md
문서   # ADR-001. 출전 주제를 보이스피싱 사후 대응으로 한다
참조   "ADR-001 참조"
```

- 3자리 순번이며 **재사용하지 않습니다.**
- slug은 영문 kebab-case, 문서 안 H1은 한국어.
- 새 ADR을 쓰면 **아래 목록에 등록**합니다.

`spec/`·`docs/`는 다른 규칙(`MM-dd-{slug}`)을 씁니다 — 폴더 성격이 달라서입니다
(→ [ADR-006](006-artifacts-and-numbering.md)).

## 템플릿

```markdown
# ADR-NNN. <결정 한 문장>

- 상태: 제안 | 채택 | 폐기 | 대체됨(→ ADR-NNN)
- 날짜: YYYY-MM-DD
- 관련 문서: <../spec/..., ../rfc/..., ../docs/... 등>

## 맥락

무엇을 정해야 했고, 그때 무엇을 알고 있었는지. 제약(시간·인원·데이터 접근)도 여기.

## 결정

무엇으로 정했는지.

## 근거

왜 그것인지. 탈락한 대안과 탈락 사유를 함께.

## 결과

이 결정으로 무엇이 쉬워지고 무엇이 어려워지는지. 나중에 재검토할 트리거가 있으면 명시.
```

## 목록

| 번호 | 결정 | 상태 |
| --- | --- | --- |
| [001](001-topic-selection.md) | 출전 주제를 보이스피싱 사후 대응으로 | 채택 |
| [002](002-project-name.md) | 프로젝트 이름을 FinAlly로 | 채택 |
| [003](003-spec-layout.md) | spec을 common·backend·frontend로 나누고 파일명을 `MM-dd-{title}.md`로 | 채택 |
| [004](004-agenda-and-assets.md) | AGENDA.md를 `docs/context/`로 옮기고 `assets/` 신설 | 채택 |
| [005](005-filename-convention.md) | 파일명 규약을 전 폴더로 통일하고 번호를 문서 안 ID로 분리 | 부분 대체됨(→006) |
| [006](006-artifacts-and-numbering.md) | HTML 아티팩트를 `assets/`로 분리하고 RFC·ADR 파일명에 번호 복원 | 채택 |
| [007](007-architecture-doc.md) | 시스템 구조를 최상위 `ARCHITECTURE.md`에 둔다 | 채택 |
| [008](008-structure-gate-ci.md) | 폴더 구조가 바뀌면 RFC 수정을 CI로 강제한다 | 채택 |
| [009](009-restore-mapping-location.md) | 복원 매핑을 암호화해 서버 볼트에 두고 키는 클라이언트가 갖는다 | 채택 |
| [010](010-case-store.md) | 사건 상태를 관계형 DB에, 매핑은 볼트에, 원본은 객체 저장소에 둔다 | 부분 대체됨(→016) |
| [011](011-pii-boundary-hardening.md) | 기관명은 토큰화하지 않고, 복원 가능 위치는 코드가 지정한다 | 채택 |
| [012](012-kb-collection.md) | 절차 지식을 4개 소스에서 하루 1회 수집해 조문 단위로 저장한다 | 채택 |
| [013](013-chat-context.md) | 챗은 도구를 쓰지 않고, 챗 답변의 토큰은 종류별로 부분 복원한다 | 채택 |
| [014](014-module-names.md) | 모듈 이름을 영어 한 층으로 정하고 동작을 네 층으로 나눈다 | 채택 |
| [015](015-citation-and-reask.md) | 참조 번호를 프롬프트 자료 전체에 붙이고, 근거가 없으면 되묻는다 | 채택 |
| [016](016-retention-and-datastore.md) | 사건 보관을 마지막 활동일 기준 180일로, 저장소를 Supabase(서울)로 | 채택 |
| [017](017-doc-integrity-ci.md) | 문서 간 참조가 규약과 어긋나면 CI로 막는다 | 채택 |
| [018](018-inventory-skill.md) | 저장소를 조회하는 도구를 스킬로 두고, 정본을 복제하지 않는다 | 채택 |

## 아직 안 쓴 것

- [x] ~~백엔드 모듈 경계와 역할~~ — [ADR-010](010-case-store.md)·[ADR-012](012-kb-collection.md)·[ADR-013](013-chat-context.md)·[ADR-014](014-module-names.md) 로 나뉘어 결정됨
- [x] ~~사건 보존 기간~~ — [ADR-010](010-case-store.md)으로 한 층으로 통일하고,
      [ADR-016](016-retention-and-datastore.md)으로 **마지막 활동일부터 180일** 확정 (핸드오프 ①)
- [ ] 재진입·식별 모델 — `User` 테이블의 존재 여부가 걸립니다 (핸드오프 ②). **아직 미정**
- [ ] 데스크톱 웹 우선 · 모바일 P1
- [ ] 기술 스택 확정 (기획서 §9는 예정안)
- [ ] MVP 범위 컷 — P0/P1 경계를 대회 일정에 맞춰 재확정
