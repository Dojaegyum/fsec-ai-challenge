# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# 에디터
.vscode/
.idea/
*.swp

# 작업 중 임시본
*.tmp
*.bak
~$*

# 참고용으로 들여온 외부·사내 자료 — 저장소에 올리지 않습니다.
# 구조만 참고하고, 내용은 이 저장소 밖에 둡니다.
manual_ex.json
*_ex.json

# 의존성·빌드 산출물 (src/.gitignore와 중복이지만 안전망)
node_modules/
.next/
out/
.vercel
*.tsbuildinfo
.env*

# Python 캐시 — 검사기를 직접 import 하면 생깁니다
__pycache__/
*.pyc

# 브레인스토밍 비주얼 컴패니언 세션 파일 — 저장소에 올리지 않습니다
.superpowers/

# DART 전체 법인 목록 캐시 (11MB · 매일 바뀜) — dart_lookup.py 가 받아 둡니다
assets/datasets/08-26-org-dict/.dart-corp.zip

# 시연용 합성 자료의 중간 산출물 — `make_mock.py` 가 다시 만듭니다
assets/demo/*/raw/
